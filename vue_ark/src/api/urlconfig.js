const url = 'http://localhost:3000';
const urls= class{
    static m (){
        //接口
        const get_character = `${url}/get_character`
        const get_character_list = `${url}/get_character_list`
        const get_character_one = `${url}/get_character_one`
        const get_skill_f_one = `${url}/get_skill_f_one`
        const get_skill_b_one = `${url}/get_skill_b_one`
        const get_character_pro2 = `${url}/get_character_pro2`
        const get_profession = `${url}/get_profession`
        const get_profession_2 = `${url}/get_profession_2`
        const get_camp = `${url}/get_camp`
        const get_orgin = `${url}/get_orgin`
        const get_race = `${url}/get_race`
        const get_activity_0 = `${url}/get_activity_0`
        const get_modeinit = `${url}/get_modeinit`
        const get_modeinit_code = `${url}/get_modeinit_code`
        const get_tanlent_staff = `${url}/get_tanlent_staff`
        const get_activate = `${url}/get_activate`
        const get_place = `${url}/get_place`
        const add_profession_2 = `${url}/add_profession_2`
        const add_camp = `${url}/add_camp`
        const add_activity = `${url}/add_activity`
        const add_character = `${url}/add_character`
        const add_modeinit = `${url}/add_modeinit`
        const add_modesec = `${url}/add_modesec`
        const update_character = `${url}/update_character`
        const get_clothes_class = `${url}/get_clothes_class`
        const get_clothes = `${url}/get_clothes`
        const get_clothes_b = `${url}/get_clothes_b`
        const get_character_tag = `${url}/get_character_tag`
        const get_glory_0 = `${url}/get_glory_0`
        const get_glory_1 = `${url}/get_glory_1`
        const get_glory0 = `${url}/get_glory0`
        const get_glory1_0 = `${url}/get_glory1_0`
        return{
            get_character,
            get_character_list,
            get_character_one,
            get_profession,
            get_profession_2,
            get_activity_0,
            add_profession_2,
            add_camp,
            add_activity,
            add_character,
            add_modeinit,
            add_modesec,
            get_camp,
            get_orgin,
            get_race,
            get_place,
            get_modeinit_code,
            get_modeinit,
            get_character_pro2,
            get_tanlent_staff,
            update_character,
            get_activate,
            get_clothes_class,
            get_clothes,
            get_clothes_b,
            get_character_tag,
            get_glory_0,
            get_glory_1,
            get_glory0,
            get_glory1_0,
        }
    }
}

export default urls